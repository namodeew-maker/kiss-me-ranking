# PostgreSQL connection
DB_USER=postgres
DB_HOST=localhost
DB_NAME=lotto_project
DB_PASSWORD=your_password_here
DB_PORT=5432

# Or use a full connection string (for cloud hosting like Render/Railway)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Server port
PORT=3000
