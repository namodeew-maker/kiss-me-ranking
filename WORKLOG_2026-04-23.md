# Worklog 2026-04-23

## เป้าหมายของงานวันนี้
- ตรวจสอบ capacity ของระบบ `ranking.kissme-vip.com` ที่รันบน VPS `185.182.184.180`
- ระบุคอขวดของ production ปัจจุบัน
- ปรับโค้ดและ config ใน repo ให้พร้อมรองรับเป้าหมายประมาณ `100-300 concurrent users`
- deploy ขึ้น VPS จริง
- จูน `PM2`, `Nginx`, และ OS เพิ่ม
- ยิง benchmark ซ้ำหลังปรับระบบ

## สรุปปัญหาที่เจอก่อนแก้
- แอปรันแบบ `single instance`
- admin auth/session ผูกกับ memory ของ process ทำให้ scale หลาย instance ได้ไม่ดี
- มี blanket rate limit ฝั่ง `/api/*`
- PostgreSQL pool ยังใช้ค่า default เป็นหลัก
- endpoint กลุ่ม ranking อ่านบ่อยแต่ยังไม่มี micro-cache ฝั่ง origin
- schema ยังขาด index สำคัญบางตัว โดยเฉพาะตาราง `transactions`
- Nginx/OS ยังไม่ได้จูนสำหรับ burst traffic มากนัก

## สิ่งที่แก้ใน repo

### 1. `server.js`
- เปลี่ยน admin auth เป็น signed stateless token ด้วย `crypto`
- ตัดการพึ่งพา in-memory token store เพื่อให้รองรับหลาย PM2 instances ได้
- เพิ่มการบังคับใช้ `AUTH_SESSION_SECRET` ใน production
- เพิ่ม config สำหรับ PostgreSQL pool ผ่าน env เช่น:
  - `PG_POOL_MAX`
  - `PG_IDLE_TIMEOUT_MS`
  - `PG_CONNECTION_TIMEOUT_MS`
  - `PG_QUERY_TIMEOUT_MS`
  - `PG_STATEMENT_TIMEOUT_MS`
  - `PG_MAX_USES`
- แยก rate limit เป็นหลายกลุ่ม:
  - read API
  - write API
  - login API
- เพิ่ม cache headers ให้ public endpoints หลัก
- เพิ่ม runtime index creation สำหรับ index สำคัญในฐานข้อมูล

### 2. `init-db.sql`
- เพิ่ม index สำหรับ query ที่โตเร็วและถูกเรียกบ่อย เช่นบน `transactions` และ `lottery_guesses`

### 3. deploy config
- ปรับ `deploy/ecosystem.config.js` และ `deploy/ecosystem.cohost-kissme-for-web.config.js`
  - เปลี่ยนเป็น `cluster mode`
  - ตั้ง `instances = 3`
- ปรับ `.env` template ใน `deploy/` และ `.env.example`
  - เพิ่ม env สำหรับ auth secret
  - เพิ่ม env สำหรับ pool / rate limit / cache
  - ทำให้ค่าพอร์ต production ตรงกับของจริงที่ `3010`

### 4. Nginx config ใน repo
- ปรับ `deploy/nginx-ranking.conf`
- ปรับ `deploy/nginx-ranking.cohost-kissme-for-web.conf`
- เพิ่ม:
  - upstream keepalive
  - proxy buffering
  - micro-cache สำหรับ public APIs
  - static cache ที่เหมาะกับ asset

## สิ่งที่ทำบน VPS จริง
- ยืนยันว่า production รันบน `:3010`
- backup config/app เดิมไว้ที่:
  - `/root/kissme-ranking-backups/20260423-211033`
  - `/root/kissme-ranking-backups/20260423-162342`
- อัปโหลดไฟล์ที่แก้ขึ้นไปยัง `/var/www/kiss-me-ranking`
- เติม env ใหม่ใน `/var/www/kiss-me-ranking/.env`
- สร้าง cache dir สำหรับ Nginx:
  - `/var/cache/nginx/kiss-me-ranking`
- restart/reload service ที่เกี่ยวข้อง

## การเปลี่ยนแปลง production หลัง deploy
- `kiss-me-ranking` เปลี่ยนจาก single process เป็น `PM2 cluster 3 instances`
- public read API ได้ cache header และ Nginx micro-cache
- rate limit read API ถูกขยายขึ้น
- index ใหม่ใน PostgreSQL ถูกสร้างครบ
- `pm2-root.service` ถูกทำให้กลับมา `active`

## การจูน Nginx และ OS บน VPS

### Nginx
- ปรับ `worker_connections` เป็น `4096`
- เปิด `multi_accept on`
- เพิ่ม `keepalive`
- เพิ่ม `gzip`
- เพิ่ม `open_file_cache`
- เพิ่ม micro-cache สำหรับ:
  - `/api/staffs`
  - `/api/round`
  - `/api/sold-out`
  - `/api/ranking/*`

### systemd / OS
- เพิ่ม `LimitNOFILE=262144` ให้ nginx
- ปรับ `sysctl` หลัก ๆ เช่น:
  - `net.core.somaxconn = 8192`
  - `net.core.netdev_max_backlog = 8192`
  - `net.ipv4.tcp_max_syn_backlog = 8192`
  - `net.ipv4.tcp_fin_timeout = 15`
  - `net.ipv4.tcp_tw_reuse = 1`

## ผล benchmark หลังปรับ

### Raw Node ตรง
- `/api/ranking/customers` ประมาณ `17 req/s`
- `/api/ranking/staff` ประมาณ `19 req/s`
- `/api/ranking/staff-usage` ประมาณ `45 req/s`
- `/api/users/:platform_id/history` ประมาณ `2.1 req/s`
- `/api/users/:platform_id/progress` ประมาณ `6 req/s`

### ผ่าน Nginx origin หลัง cache
- `/api/ranking/customers` ประมาณ `51 req/s`
- `/api/staffs` ประมาณ `58 req/s`
- `/api/ranking/staff-usage` ประมาณ `158 req/s`

### ภาพรวมทรัพยากรเครื่อง
- VPS: `6 vCPU`, RAM ประมาณ `12 GB`
- หลัง benchmark เครื่องยังเหลือ RAM มากและไม่ได้ตันที่ CPU
- bottleneck หลักเริ่มขยับไปที่:
  - endpoint personalized
  - latency/query ของ PostgreSQL
  - per-IP rate limit

## ประเมิน capacity หลังจบรอบนี้
- public/read-heavy traffic: ประมาณ `200-300 concurrent users` มีความเป็นไปได้มากขึ้น
- mixed real usage: ประมาณ `100-180 concurrent users` แบบ conservative
- ถ้าผู้ใช้จำนวนมากยิง endpoint personalized หนัก ๆ พร้อมกัน เพดานจริงจะต่ำกว่านี้

## ไฟล์ใน repo ที่แก้วันนี้
- `.env.example`
- `server.js`
- `init-db.sql`
- `deploy/.env.production`
- `deploy/.env.cohost-kissme-for-web.production`
- `deploy/ecosystem.config.js`
- `deploy/ecosystem.cohost-kissme-for-web.config.js`
- `deploy/nginx-ranking.conf`
- `deploy/nginx-ranking.cohost-kissme-for-web.conf`
- `deploy/setup-vps.sh`

## งานที่ควรทำต่อ
- optimize endpoint personalized โดยเฉพาะ `history` และ `progress`
- ทดสอบ write/upload flow ภายใต้ load จริง
- พิจารณาแยกหรือผ่อน per-IP rate limit กรณีมีผู้ใช้หลัง NAT/IP เดียวกันจำนวนมาก
- ทำ benchmark ระดับ `300-500 concurrent` เพิ่มเพื่อหาจุดเริ่มตันจริง
