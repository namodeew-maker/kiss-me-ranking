# Excel Admin Guide

คู่มือนี้อธิบายวิธีใช้ฟีเจอร์ `Excel Export / Import` ในหน้าแอดมินของโปรเจกต์ `Kiss Me Ranking`

## เป้าหมาย

ฟีเจอร์นี้มีไว้เพื่อ:

- ดาวน์โหลดรายงานออกไปใช้ใน Excel
- ดาวน์โหลดไฟล์แบบแก้ไขได้เฉพาะส่วนที่ปลอดภัย
- อัปโหลดไฟล์กลับเข้าระบบโดยมี validation และ rollback กันข้อมูลพัง

## รายงานที่รองรับ

### 1. `leaderboard`

ใช้สำหรับดูอย่างเดียว

- ดาวน์โหลดได้เป็น `CSV`
- ดาวน์โหลดได้เป็น `XLSX`
- ห้าม import กลับเข้าระบบ

เหตุผล:

- เป็นข้อมูลคำนวณจากระบบ
- ไม่ควรแก้ไขจาก Excel โดยตรง

### 2. `members`

ใช้ได้ทั้งดูรายงานและแก้ข้อมูลบางส่วน

- ดาวน์โหลดได้เป็น `CSV`
- ดาวน์โหลดได้เป็น `XLSX`
- ดาวน์โหลดไฟล์แก้ไขได้เป็น `members-editable.xlsx`
- import กลับเข้าระบบได้

สิ่งที่อนุญาตให้แก้:

- `editable_display_name`
- `editable_picture_url`

สิ่งที่ไม่ควรแก้:

- `member_id`
- `line_user_id`
- `total_score`
- `available_points`
- `tier_name`
- คอลัมน์ระบบอื่นทั้งหมด

### 3. `reward_claims_current`

ใช้ดู snapshot สิทธิ์รางวัลล่าสุด และใช้สร้าง claim ใหม่จาก Excel

- ดาวน์โหลดได้เป็น `CSV`
- ดาวน์โหลดได้เป็น `XLSX`
- ดาวน์โหลดไฟล์แก้ไขได้เป็น `reward_claims_current-editable.xlsx`
- import กลับเข้าระบบได้

สิ่งที่อนุญาตให้กรอกจากไฟล์แก้ไข:

- `row_action`
- `claim_amount`
- `claim_mode`
- `claim_note`
- `redeemed_at`

ข้อสำคัญ:

- ใช้ `row_action=claim` เพื่อสร้างรายการใช้สิทธิ์ใหม่เท่านั้น
- ห้ามแก้คอลัมน์ snapshot เดิม เช่น `total_amount`, `remaining_amount`, `reward_type`

## วิธีใช้งานจากหน้าแอดมิน

ไปที่แท็บ:

`👥 จัดการ User`

จะมีแผง `Excel Export / Import`

ในแผงนี้จะมี:

- ปุ่ม `เปิดคู่มือการใช้งาน`
- ส่วนดาวน์โหลดรายงาน
- ส่วนดาวน์โหลดไฟล์แก้ไข
- ส่วนอัปโหลดไฟล์กลับ
- ตาราง `ประวัติการ Import ล่าสุด`

## การดาวน์โหลดรายงาน

### ดาวน์โหลด CSV

1. เลือกรายงานในช่อง `ดาวน์โหลดรายงาน`
2. กด `ดาวน์โหลด CSV`

เหมาะสำหรับ:

- เปิดดูเร็ว
- ส่งต่อให้ทีม
- ใช้กับงานสรุปหรือ import เข้าเครื่องมืออื่น

### ดาวน์โหลด XLSX

1. เลือกรายงานในช่อง `ดาวน์โหลดรายงาน`
2. กด `ดาวน์โหลด XLSX`

เหมาะสำหรับ:

- เปิดใน Excel
- จัดรูปแบบต่อ
- ส่งให้ผู้บริหารหรือทีมตรวจสอบ

## การดาวน์โหลดไฟล์แก้ไข

### สำหรับ `members`

1. เลือก `Members`
2. กด `ดาวน์โหลดไฟล์แก้ไข`
3. เปิดไฟล์ใน Excel
4. แก้เฉพาะคอลัมน์:
   - `editable_display_name`
   - `editable_picture_url`

### สำหรับ `reward_claims_current`

1. เลือก `Reward Claims Current`
2. กด `ดาวน์โหลดไฟล์แก้ไข`
3. เปิดไฟล์ใน Excel
4. กรอกเฉพาะแถวที่ต้องการทำรายการ

ตัวอย่าง:

- `row_action` = `claim`
- `claim_amount` = `500`
- `claim_mode` = `reuse` หรือ `withdraw`
- `claim_note` = ข้อความอธิบาย
- `redeemed_at` = `2026-04-18`

## การอัปโหลดไฟล์กลับเข้าระบบ

### Import `members`

1. เลือก `Members` ในช่องอัปโหลด
2. เลือกไฟล์ที่ได้จาก `ดาวน์โหลดไฟล์แก้ไข`
3. กด `อัปโหลดไฟล์กลับ`
4. ยืนยันการทำรายการ

ผลลัพธ์:

- ระบบจะอัปเดตชื่อหรือรูปภาพของผู้ใช้
- ถ้ามีหลายบัญชีที่ผูก `global_user_id` เดียวกัน ระบบจะอัปเดตทั้งชุด

### Import `reward_claims_current`

1. เลือก `Reward Claims Current` ในช่องอัปโหลด
2. เลือกไฟล์ที่ได้จาก `ดาวน์โหลดไฟล์แก้ไข`
3. กด `อัปโหลดไฟล์กลับ`
4. ยืนยันการทำรายการ

ผลลัพธ์:

- ระบบจะสร้างรายการ claim ใหม่จากแถวที่ตั้ง `row_action=claim`
- ระบบจะเช็กยอดคงเหลือก่อนเสมอ

## กฎความปลอดภัย

ระบบตั้งใจออกแบบให้ปลอดภัยก่อนความสะดวก

### 1. ถ้ามีแถวผิดแม้แถวเดียว ระบบจะ rollback ทั้งไฟล์

หมายความว่า:

- ไม่มีการบันทึกบางส่วน
- ไม่มีข้อมูลครึ่ง ๆ กลาง ๆ
- ต้องแก้ไฟล์ให้ผ่านทั้งหมดก่อนค่อย import ใหม่

### 2. ใช้เฉพาะไฟล์จากระบบเท่านั้น

ไม่ควรสร้างไฟล์เองจากศูนย์ เพราะ:

- header อาจไม่ตรง
- primary key อาจหาย
- คอลัมน์ action อาจผิดรูปแบบ

### 3. อย่าแก้คอลัมน์ id หรือคอลัมน์คำนวณ

เช่น:

- `member_id`
- `lottery_guess_id`
- `total_score`
- `remaining_amount`

### 4. `leaderboard` เป็น read-only เสมอ

ไม่สามารถ import กลับได้

## รูปแบบข้อมูลที่สำคัญ

### `redeemed_at`

ต้องเป็นรูปแบบ:

```text
YYYY-MM-DD
```

ตัวอย่าง:

```text
2026-04-18
```

### `claim_mode`

สำหรับ cashback ใช้ได้เฉพาะ:

- `reuse`
- `withdraw`

### `row_action`

สำหรับ `reward_claims_current` ตอนนี้รองรับ:

- `claim`

## URL ฝั่ง backend ที่เกี่ยวข้อง

### ดาวน์โหลดรายงาน

- `GET /api/admin/export/leaderboard.csv`
- `GET /api/admin/export/leaderboard.xlsx`
- `GET /api/admin/export/members.csv`
- `GET /api/admin/export/members.xlsx`
- `GET /api/admin/export/reward_claims_current.csv`
- `GET /api/admin/export/reward_claims_current.xlsx`

### ดาวน์โหลดไฟล์แก้ไข

- `GET /api/admin/export/members-editable.xlsx`
- `GET /api/admin/export/reward_claims_current-editable.xlsx`

### อัปโหลดไฟล์กลับ

- `POST /api/admin/import/members`
- `POST /api/admin/import/reward_claims_current`

## ปัญหาที่พบบ่อย

### อัปโหลดแล้วขึ้นว่า import ไม่สำเร็จ

ให้เช็ก:

- ใช้ไฟล์จากระบบหรือไม่
- header ถูกแก้ไขหรือไม่
- `row_action` ถูกต้องหรือไม่
- `claim_amount` มากกว่ายอดคงเหลือหรือไม่
- `redeemed_at` เป็น `YYYY-MM-DD` หรือไม่

### อยากดูว่าไฟล์ไหนเคยอัปโหลดไปแล้ว

ให้ดูที่ตาราง:

`ประวัติการ Import ล่าสุด`

ข้อมูลที่จะแสดง:

- เวลาอัปโหลด
- report ที่ใช้
- ชื่อไฟล์
- สถานะ success/failed
- จำนวนแถวที่อ่าน / ประมวลผล / บันทึกจริง
- ชื่อแอดมินที่เป็นคนอัปโหลด
- ข้อความ error แบบย่อ

### ดาวน์โหลดได้แต่เปิดไฟล์แล้วข้อมูลไม่ครบ

ให้เช็ก:

- filter ใน Excel
- ใช้ไฟล์เวอร์ชันล่าสุดหรือไม่
- รายงานนั้นมีข้อมูลจริงในระบบหรือไม่

## คำแนะนำในการใช้งานจริง

1. ถ้าต้องการแค่ดูข้อมูล ใช้ `CSV` หรือ `XLSX` ปกติ
2. ถ้าต้องการแก้ข้อมูล ใช้ปุ่ม `ดาวน์โหลดไฟล์แก้ไข` เท่านั้น
3. อย่า copy ข้ามไฟล์จน header หรือ column order เปลี่ยน
4. ทดสอบกับจำนวนน้อยก่อน โดยเฉพาะ `reward_claims_current`
5. ถ้ามี error ให้แก้ทั้งไฟล์แล้วอัปโหลดใหม่ เพราะระบบ rollback ทั้งก้อน
